# Rizam MD

Editor Markdown berbasis React dengan preview langsung, formatter, autosave lokal, import, dan download file `.md`.

## Scripts

```bash
npm install
npm run dev
npm run build
```

## Deploy Cloudflare Pages

Build command:

```bash
npm run build
```

Output directory:

```text
dist
```
